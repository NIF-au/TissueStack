{
    "name": "test",
    "version": "0.0.1",

    "description": "test",
    "author": "tissuestack",
    "repository": {
      "type": "git",
      "url": "https://github.com/NIF-au/TissueStack.git"
    },
    "license": "GPL-3.0",

    "scripts": {
        "dev": "UV_THREADPOOL_SIZE=100 webpack-dev-server --config webpack.config.js --content-base ./src --hot --inline --progress --devtool eval",
        "prod": "UV_THREADPOOL_SIZE=100 webpack -p --config webpack.config.js --progress && cp src/index.html build && cp src/tissuestack.css build"
    },

    "devDependencies": {
        "babel-core": ">=6.x",
        "babel-loader": ">=6.x",
        "bundle-loader": ">=0.5.4",
        "babel-preset-es2015" : ">=6.x",
        "babel-preset-es2015-loose": ">=7.0.x",
        "babel-preset-stage-1": ">=6.5.x",
        "webpack": ">=1.13.x",
        "webpack-dev-server": ">=1.14.x"
    },

    "dependencies": {
        "jquery" : ">=3.1.x"
    }
}
