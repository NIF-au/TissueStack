#include "common.h"

tissuestack::common::RequestFilter::RequestFilter()
{
	// nothing to be done, after all we are abstract
};

tissuestack::common::RequestFilter::~RequestFilter()
{
	// nothing to be done, after all we are abstract
};
