##################################################################################
######################   TISSUE STACK - IMAGE SERVER  [in C] #####################
##################################################################################

In order to compile the Tissue Stack Image Server, type
 
                "make VERSION=X.X"

where X.X is meant to denote the version, e.g. 1.1.

Note: 
------
You will be prompted with sudo since the default install directories are:

/usr/local/tissuestack/X.X/bin      [for TissueStackImageServer executable]
/usr/local/tissuestack/X.X/lib      [for the JNI lib: libTissueStack.so   ]
/usr/local/tissuestack/X.X/plugins  [for the ImageServer plugins          ]
